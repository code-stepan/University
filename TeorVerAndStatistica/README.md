# Дискретные случайные величины

Приложение для работы с дискретными случайными величинами: создание, редактирование, визуализация и расчет статистических характеристик.

## Возможности

- 📊 **Работа с дискретными случайными величинами**
  - Создание и редактирование распределений
  - Автоматическая нормализация вероятностей
  - Проверка корректности данных

- 🔢 **Статистические характеристики**
  - Математическое ожидание
  - Дисперсия и стандартное отклонение
  - Коэффициент асимметрии (skewness)
  - Коэффициент эксцесса (kurtosis)

- 📈 **Визуализация**
  - Закон распределения (столбчатая диаграмма)
  - Полилайн распределения
  - Функция распределения (CDF)

- ➕ **Математические операции**
  - Сложение двух случайных величин
  - Умножение на скаляр
  - Умножение двух случайных величин

- 💾 **Сохранение и загрузка**
  - Сохранение в JSON формате
  - Загрузка из JSON файлов

- 🖥️ **Графический интерфейс**
  - Удобное редактирование значений и вероятностей
  - Визуализация графиков в реальном времени
  - Табличное представление данных

## Установка

### Требования

- Python 3.7 или выше
- PyQt6 >= 6.4.0
- matplotlib >= 3.5.0
- numpy >= 1.21.0

### Установка зависимостей

```bash
pip install -r requirements.txt
```

Или установите вручную:

```bash
pip install PyQt6>=6.4.0 matplotlib>=3.5.0 numpy>=1.21.0
```

## Использование

### Графический интерфейс (GUI)

Запустите приложение:

```bash
python main.py
```

**Основные функции:**

1. **Добавление значений:**
   - Введите значение и вероятность в поля ввода
   - Нажмите кнопку "Добавить"
   - Вероятности автоматически нормализуются

2. **Редактирование:**
   - Измените значения напрямую в таблице
   - Изменения применяются автоматически

3. **Загрузка файла:**
   - Меню "Файл" → "Загрузить"
   - Выберите JSON файл из папки `test_data/`

4. **Сохранение:**
   - Меню "Файл" → "Сохранить" или "Сохранить как..."
   - Файл сохраняется в JSON формате

5. **Статистика:**
   - Меню "Операции" → "Статистика"
   - Или нажмите кнопку "Статистика"

6. **Графики:**
   - Автоматически обновляются при изменении данных
   - Доступны три вида графиков на вкладках

### Консольное использование

#### Базовые примеры

```bash
python examples/basic_usage.py
```

Демонстрирует:
- Создание случайных величин
- Операции (сложение, умножение)
- Сохранение и загрузку

#### Продвинутые примеры

```bash
python examples/advanced_operations.py
```

Демонстрирует:
- Умножение двух случайных величин
- Биномиальное распределение
- Асимметрия и эксцесс
- Цепочки операций

#### Тестирование файлов

```bash
python examples/test_load_files.py
```

Тестирует загрузку всех JSON файлов из папки `test_data/`.

### Программное использование

```python
from src.core.discrete_random_variable import DiscreteRandomVariable
from src.core.statistics import StatisticsCalculator

# Создание случайной величины
drv = DiscreteRandomVariable([
    (1, 0.3),
    (2, 0.5),
    (3, 0.2)
])

# Статистика
stats = StatisticsCalculator.get_all_statistics(drv)
print(f"Математическое ожидание: {stats['expectation']}")
print(f"Дисперсия: {stats['variance']}")

# Операции
X = DiscreteRandomVariable([(1, 0.5), (2, 0.5)])
Y = DiscreteRandomVariable([(3, 0.4), (4, 0.6)])

# Сложение
Z = X + Y

# Умножение на скаляр
W = 2 * X

# Умножение двух величин
V = X * Y
```

## Структура проекта

```
теорвер/
├── main.py                      # Точка входа приложения
├── requirements.txt             # Зависимости
├── README.md                    # Этот файл
│
├── src/                         # Исходный код
│   ├── core/                    # Основная логика
│   │   ├── discrete_random_variable.py  # Класс случайной величины
│   │   └── statistics.py                # Статистические расчеты
│   │
│   ├── gui/                     # Графический интерфейс
│   │   ├── main_window.py       # Главное окно
│   │   ├── variable_editor.py   # Редактор переменных
│   │   └── plot_widget.py       # Виджет графиков
│   │
│   ├── visualization/           # Визуализация
│   │   ├── distribution_plot.py # Закон распределения
│   │   ├── polyline_plot.py     # Полилайн
│   │   └── cdf_plot.py          # Функция распределения
│   │
│   └── io/                      # Ввод/вывод
│       └── serialization.py     # Сериализация в JSON
│
├── examples/                    # Примеры использования
│   ├── basic_usage.py           # Базовые примеры
│   ├── advanced_operations.py   # Продвинутые примеры
│   ├── test_load_files.py       # Тестирование загрузки
│   └── README.md                # Инструкции по примерам
│
└── test_data/                   # Тестовые данные
    ├── dice.json                # Бросание кубика
    ├── coin_toss.json           # Подбрасывание монеты
    ├── binomial_5_03.json       # Биномиальное распределение
    ├── custom_distribution.json # Произвольное распределение
    ├── two_dice_sum.json        # Сумма двух кубиков
    ├── poisson_like.json        # Пуассоновское распределение
    └── README.md                # Описание тестовых файлов
```

## Формат JSON файлов

Файлы сохраняются в следующем формате:

```json
{
  "pmf": [
    [значение1, вероятность1],
    [значение2, вероятность2],
    ...
  ]
}
```

**Пример:**

```json
{
  "pmf": [
    [1, 0.16666666666666666],
    [2, 0.16666666666666666],
    [3, 0.16666666666666666],
    [4, 0.16666666666666666],
    [5, 0.16666666666666666],
    [6, 0.16666666666666666]
  ]
}
```

## Тестовые данные

В папке `test_data/` находятся готовые примеры для тестирования:

- `dice.json` - Бросание игрального кубика (равномерное распределение)
- `coin_toss.json` - Подбрасывание монеты
- `binomial_5_03.json` - Биномиальное распределение B(5, 0.3)
- `custom_distribution.json` - Произвольное распределение
- `two_dice_sum.json` - Сумма двух кубиков
- `poisson_like.json` - Распределение, напоминающее Пуассоновское

## Математические операции

### Сложение случайных величин

Если X и Y - независимые случайные величины, то Z = X + Y имеет распределение:

P(Z = z) = Σ P(X = x) · P(Y = z - x)

### Умножение на скаляр

Если X - случайная величина и c - константа, то Y = cX имеет распределение:

P(Y = cx) = P(X = x)

### Умножение случайных величин

Если X и Y - независимые случайные величины, то Z = X · Y имеет распределение:

P(Z = z) = Σ P(X = x) · P(Y = z/x), где x ≠ 0

## Статистические характеристики

- **Математическое ожидание:** E[X] = Σ x · P(X = x)
- **Дисперсия:** Var(X) = E[X²] - (E[X])²
- **Стандартное отклонение:** σ = √Var(X)
- **Коэффициент асимметрии:** Skew = E[((X - μ)/σ)³]
- **Коэффициент эксцесса:** Kurt = E[((X - μ)/σ)⁴] - 3

## Возможные проблемы и решения

### Проблема: Ошибка при импорте PyQt6

**Решение:** Убедитесь, что PyQt6 установлен:
```bash
pip install PyQt6
```

### Проблема: Вероятности нормализуются автоматически

**Особенность:** При добавлении значений вероятности автоматически нормализуются, чтобы их сумма была равна 1. Это правильное поведение для корректного распределения.

### Проблема: Ошибка при загрузке файла

**Решение:** 
- Проверьте формат JSON файла
- Убедитесь, что сумма вероятностей близка к 1.0
- Проверьте, что все значения и вероятности - числа

## Разработка

### Запуск тестов

```bash
# Тест загрузки файлов
python examples/test_load_files.py

# Базовые примеры
python examples/basic_usage.py

# Продвинутые примеры
python examples/advanced_operations.py
```

## Лицензия

Этот проект создан в учебных целях.

## Автор

Разработано для работы с дискретными случайными величинами в рамках курса теории вероятностей.

---

**Приятной работы с дискретными случайными величинами! 📊**

